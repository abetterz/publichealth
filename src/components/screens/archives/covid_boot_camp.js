import React from "react";
import { connect } from "react-redux";

export const CovidBootCamp = (props) => {
  return <div> CovidBootCamp</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(CovidBootCamp);
