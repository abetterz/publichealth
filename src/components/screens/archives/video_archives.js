import React from "react";
import { connect } from "react-redux";

export const VideoArchives = (props) => {
  return <div> video_archives</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(VideoArchives);
