import React from "react";
import { connect } from "react-redux";

export const Covid19Overview = (props) => {
  return <div> covid_19_overview</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(Covid19Overview);
