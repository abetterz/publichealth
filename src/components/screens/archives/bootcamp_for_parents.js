import React from "react";
import { connect } from "react-redux";

export const BootCampForParents = (props) => {
  return <div> BootCampForParents</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(BootCampForParents);
